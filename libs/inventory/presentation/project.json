{
  "name": "inventory-presentation",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/inventory/presentation/src",
  "projectType": "library",
  "tags": ["domain:inventory", "layer:presentation"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/inventory/presentation/jest.config.ts"
      }
    }
  }
}