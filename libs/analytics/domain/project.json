{
  "name": "analytics-domain",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/analytics/domain/src",
  "projectType": "library",
  "tags": ["domain:analytics", "layer:domain"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/analytics/domain/jest.config.ts"
      }
    }
  }
}