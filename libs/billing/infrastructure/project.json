{
  "name": "billing-infrastructure",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/billing/infrastructure/src",
  "projectType": "library",
  "tags": ["domain:billing", "layer:infrastructure"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/billing/infrastructure/jest.config.ts"
      }
    }
  }
}